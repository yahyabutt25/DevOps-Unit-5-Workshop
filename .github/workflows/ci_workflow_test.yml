name: Continuous Intergration
on: [push]

jobs:
    build:
        name: Build and test
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4

            - name: Hello World
            - run: echo "Hello, world!"